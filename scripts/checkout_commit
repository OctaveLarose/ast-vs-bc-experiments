#!/bin/bash
set -e
SCRIPT_PATH=`dirname $0`
source ${SCRIPT_PATH}/script.inc

# $1 target-path
# $2 commit-id
INFO Checking out "$2" in "$1"
pushd "$1"
git remote set-branches origin '*'
git fetch --depth=1 origin "$2"
git checkout "origin/$2" -b "local/$2"
popd > /dev/null
